config:
  target: "http://localhost:3001"
  phases:
    - duration: 60  # test duration in seconds
      arrivalRate: 20  # 20 users per second

scenarios:
  - flow:
      - get:
          url: "/api/foland-realty/property"
